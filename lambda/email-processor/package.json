{
  "name": "email-processor",
  "version": "1.0.0",
  "description": "Lambda function to process incoming emails and forward to Gmail",
  "main": "index.js",
  "scripts": {
    "test": "node --test test.js",
    "deploy": "bash deploy.sh",
    "deploy:windows": "bash deploy.sh",
    "build": "node ../package-function.mjs email-processor.zip",
    "build:windows": "node ../package-function.mjs email-processor.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1136.0",
    "@aws-sdk/client-ses": "^3.1136.0",
    "mailparser": "^3.9.28",
    "nodemailer": "^10.0.10"
  },
  "keywords": [
    "aws",
    "lambda",
    "ses",
    "email",
    "forwarding"
  ],
  "author": "Raj Kumar",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  }
}
